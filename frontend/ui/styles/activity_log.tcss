/* ActivityLog - Realtime activity log widget */
ActivityLog {
    background: #000000;
    height: 1fr;
    width: 1fr;
    overflow-y: auto;
    border: none;
    padding: 0 1;
}

ActivityLog:focus {
    border: none;
}

/* Scrollbar styling */
ActivityLog Scrollbar {
    background: #000000;
}

ActivityLog ScrollbarVertical > .vertical-scrollbar {
    background: #2a2a2a;
}

ActivityLog ScrollbarVertical > .vertical-scrollbar-thumb {
    background: #0088ff;
}

/* Empty state */
ActivityLog:empty {
    color: #888888;
    text-style: dim;
}
